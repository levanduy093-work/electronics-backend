# Configuration

Centralize application configuration here:
- Environment variable parsing (`@nestjs/config`)
- Database connection settings (MongoDB URI, options)
- Third-party service keys (payment, storage, messaging)
