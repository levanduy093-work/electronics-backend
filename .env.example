# Sample .env for electronics-backend
# Copy to .env and fill in the values below.

# Server
PORT=3000
# Port for the API. Change if 3000 is taken.

# Database
MONGO_URI=mongodb://<user>:<pass>@localhost:27017/electronics_shop?authSource=admin
# Get this from your MongoDB instance:
# - MongoDB Atlas: open Database > Connect > Drivers, copy the connection string and replace <user>/<password>/<cluster>.
# - Local Mongo: create a database user in admin DB, then plug the username/password here.

# Auth
JWT_SECRET=<random-32-char-secret>
REFRESH_SECRET=<different-random-32-char-secret>
# Generate strong secrets: `openssl rand -hex 32`.
# JWT_SECRET signs access tokens (30m), REFRESH_SECRET signs refresh tokens (30d) and must differ.

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500
# Comma-separated list of allowed front-end origins. Add your production domains when deploying.

# SMTP / OTP email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=<smtp-username>
SMTP_PASS=<smtp-password-or-app-password>
SMTP_FROM="Electronics Shop <no-reply@yourdomain.com>"
SMTP_SECURE=false
# Get SMTP_* from your email provider dashboard:
# - Gmail: enable 2FA, create an App Password, set user to your Gmail and pass to the app password, host smtp.gmail.com, port 587 (secure=false) or 465 (secure=true).
# - Other providers: copy the SMTP host/port/user/pass they supply.

# OTP config
OTP_TTL_SECONDS=600
OTP_MAX_ATTEMPTS=5
# TTL is OTP lifetime in seconds (default 10 minutes). OTP_MAX_ATTEMPTS blocks verification after too many wrong tries.

# AI (Gemini)
GEMINI_API_KEY=<your-gemini-api-key>
GEMINI_MODEL=gemini-1.5-flash
